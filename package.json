{
  "name": "auspex",
  "displayName": "Auspex - Hotspot Treemap & Behavioral Code Analysis",
  "description": "Interactive code hotspot treemap analyzing Git churn, complexity, and defect ratios directly in VS Code.",
  "version": "1.6.0",
  "publisher": "marc2016",
  "engines": {
    "vscode": "^1.90.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/marc2016/Auspex.git"
  },
  "categories": [
    "Visualization",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "treemap",
    "hotspots",
    "git churn",
    "code metrics",
    "visualization"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "icon": "assets/icon.png",
  "contributes": {
    "commands": [
      {
        "command": "auspex.openTreemap",
        "title": "%command.openTreemap%",
        "icon": "$(graph)"
      },
      {
        "command": "auspex.rescan",
        "title": "%command.rescan%",
        "icon": "$(refresh)"
      },
      {
        "command": "auspex.openHelp",
        "title": "%command.openHelp%",
        "icon": "$(question)"
      },
      {
        "command": "auspex.focusDetailsView",
        "title": "%command.focusDetailsView%",
        "icon": "$(info)"
      },
      {
        "command": "auspex.setJiraToken",
        "title": "%command.setJiraToken%",
        "icon": "$(key)"
      },
      {
        "command": "auspex.clearJiraToken",
        "title": "%command.clearJiraToken%",
        "icon": "$(clear-all)"
      },
      {
        "command": "auspex.testJiraConnection",
        "title": "%command.testJiraConnection%",
        "icon": "$(plug)"
      },
      {
        "command": "auspex.openSelectedFile",
        "title": "%command.openSelectedFile%",
        "icon": "$(go-to-file)"
      },
      {
        "command": "auspex.toggleEditorHealth",
        "title": "%command.toggleEditorHealth%",
        "icon": "$(heart)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "auspex.toggleEditorHealth",
          "group": "navigation@100"
        }
      ],
      "view/title": [
        {
          "command": "auspex.openTreemap",
          "when": "view == auspex.overviewView || view == auspex.sidebarView",
          "group": "navigation@1"
        },
        {
          "command": "auspex.rescan",
          "when": "view == auspex.overviewView || view == auspex.sidebarView",
          "group": "navigation@2"
        },
        {
          "command": "auspex.openHelp",
          "when": "view == auspex.overviewView || view == auspex.sidebarView",
          "group": "navigation@3"
        },
        {
          "command": "auspex.openSelectedFile",
          "when": "view == auspex.detailsView || view == auspex.healthView || view == auspex.couplingView || view == auspex.knowledgeView || (view == auspex.sidebarView && auspex.hasSelectedFile)",
          "group": "navigation@1"
        },
        {
          "command": "auspex.toggleEditorHealth",
          "when": "view == auspex.detailsView || view == auspex.healthView || view == auspex.overviewView",
          "group": "navigation@4"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "auspex-sidebar",
          "title": "%viewsContainers.activitybar.title%",
          "icon": "assets/auspex-icon.svg"
        }
      ]
    },
    "views": {
      "auspex-sidebar": [
        {
          "type": "webview",
          "id": "auspex.overviewView",
          "name": "%views.overviewView%"
        },
        {
          "type": "webview",
          "id": "auspex.sidebarView",
          "name": "%views.sidebarView%"
        },
        {
          "type": "webview",
          "id": "auspex.detailsView",
          "name": "%views.detailsView%"
        },
        {
          "type": "webview",
          "id": "auspex.healthView",
          "name": "%views.healthView%"
        },
        {
          "type": "webview",
          "id": "auspex.couplingView",
          "name": "%views.couplingView%"
        },
        {
          "type": "webview",
          "id": "auspex.knowledgeView",
          "name": "%views.knowledgeView%"
        }
      ]
    },
    "configuration": {
      "title": "Auspex",
      "properties": {
        "auspex.ignorePatterns": {
          "type": "array",
          "default": [
            "node_modules",
            "dist",
            "build",
            ".git",
            "out",
            "bin",
            "obj"
          ],
          "description": "Files and patterns to ignore during Auspex workspace analysis."
        },
        "auspex.maxCommits": {
          "type": "number",
          "default": 15000,
          "description": "Maximum number of recent Git commits to analyze for hotspots and temporal coupling (set to 0 for unlimited). Speeds up analysis in large repositories."
        },
        "auspex.authorAliases": {
          "type": "object",
          "default": {},
          "description": "Mapping of primary author names to lists of alias names or emails to consolidate Git statistics in Auspex (e.g. {\"Max Mustermann\": [\"max\", \"mmustermann\"]})."
        },
        "auspex.jira.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable Jira read-only integration to enrich commit history with ticket types and defect detection."
        },
        "auspex.jira.host": {
          "type": "string",
          "default": "",
          "description": "URL of your Jira Cloud or Jira Server instance (e.g. https://your-company.atlassian.net)."
        },
        "auspex.jira.email": {
          "type": "string",
          "default": "",
          "description": "Your Atlassian account email address (for Jira Cloud). Leave empty if using Jira Server/Data Center PAT."
        },
        "auspex.jira.bugTypes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "Bug",
            "Defect",
            "Fehler",
            "Incident",
            "Problem"
          ],
          "description": "List of Jira issue type names that represent bugs or defects in Auspex hotspot analysis."
        },
        "auspex.jira.projectKeys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Optional: Filter for specific Jira project prefixes to resolve (e.g. [\"PROJ\", \"AUS\"]). Leave empty to resolve any detected key."
        },
        "auspex.editorGutter.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show Code Health icons in the editor gutter (margin on the left next to line numbers) for functions and biomarkers."
        },
        "auspex.editorGutter.showHealthy": {
          "type": "boolean",
          "default": true,
          "description": "Show green gutter icons for healthy functions (score >= 9.0). When disabled, only warnings and critical biomarkers are shown."
        },
        "auspex.codeLens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show CodeLens annotations with Code Health score directly above function declarations in open files."
        },
        "auspex.respectGitIgnore": {
          "type": "boolean",
          "default": true,
          "description": "Respect .gitignore files when collecting and analyzing workspace files."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "compile:ext": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "compile:webview": "vite build",
    "build": "npm run compile:ext && npm run compile:webview",
    "watch:ext": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --watch",
    "package": "vsce package --no-git-tag-version",
    "test": "vitest run"
  },
  "dependencies": {
    "@mdi/js": "^7.4.47",
    "@mdi/react": "^1.6.1",
    "d3": "^7.9.0",
    "ignore": "^7.0.9",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "@types/node": "^20.14.10",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/vscode": "^1.90.0",
    "@vitejs/plugin-react": "^4.3.1",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.24.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.5.3",
    "vite": "^6.0.0",
    "vitest": "^2.1.1"
  }
}
